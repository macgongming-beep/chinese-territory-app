# 인도자 배정 재설계 v2 — 일정상세 통합 + 지도 기반 구역 배분

> **목적**: 설계 문서 (코덱스 1차 리뷰 반영한 v2).
> 인도자 배정을 별도 탭 → 일정상세로 통합하고, 구역 배분에 "지도 색칠"을 도입.
> v1 → v2 변경: draft 단일 reducer 원칙 격상 / 진입 시 서버 shared 상태 로드 /
> 빌드순서 목록먼저(리스크감소) / 비공식·식당 화면2로 이동 + 지도는 구역 전용 /
> sticky 팀바 / 지도·목록 렌더 스펙 / Undo 토스트 / 권한 / 7~10일 / 인원상한 없음.
> 작성일: 2026-05-30

---

## 0. 한 줄 요약

> "인도자 배정을 일정상세 안으로 넣는다. (1) 팀 짓기(사람) → (2) 구역 배분(지도/목록 토글) 두 화면. 모든 상태는 단일 draft에서 파생, 편집 중엔 localStorage, 마지막에 일정상세에서 '배정 공유' 한 번. 비공식/식당은 화면2 목록에서, 지도는 구역 전용."

---

## 1. 배경 / 현재 구조

### 1.1 현재 인도자 배정 (`MobileLeaderAssignment.tsx`, 1292줄)
- 별도 하단 탭 "배정" → 진입하면 오늘 일정 또 고름 (캘린더 중복)
- 3-step: 카드선택 → 팀구성 → 참가자 (step 전환 잦아 맥락 끊김)
- 모델: `team = { id, name, color, cardIds[], members[] }`,
  draft를 `localStorage(mobileLeaderAssignmentDraft:{eventId}:{user})`,
  공유상태 `draft/confirmed/shared`, 콜백은 이미 eventId 스코프
- 담당 카드 ~43개를 좁은 step1에 욱여넣음

### 1.2 일정상세 (`admin/AdminEventDetailSheet.tsx`, 697줄)
- 바텀시트. 신청자 전원 칩 strip, shared면 공유 배정 표시

### 1.3 지도 (`MapCanvas.tsx`)
- `cardBoundaries`(폴리곤), `onSelectCardBoundary(cardId)`(탭 핸들러),
  `selectedCardIds:Set`(색칠 강조), 폴리곤별 fillColor → "탭하면 팀색 칠"이 기존 인프라로 거의 됨

---

## 2. 도메인 제약 (확정)

1. 한 팀에 카드 여러 개 흔함 (구역2 / 구역+비공식) → 팀 개념 유지
2. 누구랑 누구 짝 = 인도자 수동 (자동분배 X)
3. 형식 비슷·인원 매번 다름 → "골격 복사"는 V2로 (사람은 매번 교체)
4. 하루 한 구만 감 (기흥구 OR 처인구) → 구 단위 범위 좁히기
5. 비공식·식당은 폴리곤 없음 → **지도 대상 아님, 목록에서만 배정**
6. 경계선 없는 구역카드는 인도자 배정 안 함 → 폴백 불필요
7. 관리자 카드 배정은 이번 범위 아님
8. **팀 인원 상한 없음** → 인원 수만 표시

---

## 3. 새 설계

### 3.1 전체 흐름
```
일정상세 (AdminEventDetailSheet)
  └ [팀 구성 & 배정 →]   ← 인도자/관리자만, 보기전용 인도자는 비활성
       ↓ 풀스크린 진입
  화면1: 팀 짓기 (사람만)
       ↓ 팀 탭 (또는 화면2 sticky 팀바로 전환)
  화면2: 구역 배분 (지도/목록 토글, 활성팀 단위)
       · 즉시 반영, 저장 버튼 없음 (draft 자동저장)
       ↓ 뒤로
  일정상세
    └ [배정 공유] 한 번  ← draft → shared
```

### 3.2 화면1 — 팀 짓기 (사람 전용)
```
┌─ 傳道 13:00 배정 ─────────────────┐
│ 신청 9명                           │
│ [장웅][사용자1][사용자2][사용자3]  │ ← 탭 멀티선택
│ [사용자4][사용자5][엄민석]...      │
│  ✓ 2명 선택  [→ 새 팀으로 묶기]   │
│ ═══════════════════════════════   │
│ ┌──────────────────────────────┐ │
│ │ 팀 1   장웅·사용자1        [›]│ │ ← 탭 → 화면2(그 팀)
│ │ 🗂 구역 0 · 미배정           │ │
│ ├──────────────────────────────┤ │
│ │ 팀 2   사용자2·사용자3     [›]│ │
│ └──────────────────────────────┘ │
│ 미배정 👤 사용자4 사용자5          │ ← 탭 → 팀 선택 추가
│                      [공유 →]    │
└──────────────────────────────────┘
```
- 사람 멀티선택 → "묶기" → 팀 생성
- 미배정 사람은 기존 팀 멤버 영역 탭으로 추가
- **화면1은 순수 사람만** (비공식/식당 여기 없음 — 화면2로)

### 3.3 화면2 — 구역 배분 (핵심)

#### 헤더 = sticky (엑셀 행고정)
```
╔═══════════════════════════════════╗ ← sticky 시작
║ ‹  구역 배분            📍기흥구 ▾ ║
║ ┌──────────┐┌────────┐┌────────┐ ║ ← 팀바 가로스크롤
║ │● 팀1     ││ 팀2    ││ 팀3   →│ ║   탭=활성전환
║ │장웅·사용자1││사용자2 ││사용자5 │ ║
║ │🗂신갈동4외1││⚠구역없음││...    │ ║
║ └──────────┘└────────┘└────────┘ ║
║  [🗺️ 지도]  [☰ 목록]              ║
╚═══════════════════════════════════╝ ← sticky 끝 (스크롤해도 고정)
```
- 헤더는 앱 공통 패턴(‹ + 제목) 유지 → **일관성**
- 팀바 sticky → 색칠 중 스크롤해도 활성팀·배정현황 항상 보임
- 팀 칩 = `●활성 / 팀이름 / 구성원(인원수) / 구역요약`
- 구 선택 드롭다운 (기흥구/처인구/전체)

#### 지도 모드 (구역 전용)
```
[sticky 헤더]  ← 고정
┌──────────────────────────────┐
│   🗺️ 지도 (고정창, 핀치/드래그)│ ← page 스크롤 안 함
│   ┌────┐ 팀1 진한파랑+이름     │
│   └────┘  ┌────┐ 팀2 진한초록  │
│   ░░░ 미배정(회색점선)         │
└──────────────────────────────┘
│ ⓘ 비공식·식당은 목록에서       │ ← 안내 (지도는 구역만)
```
폴리곤 상태:
| 상태 | 채움 | 라벨 |
|---|---|---|
| 미배정 | 회색 20% + 점선 | 없음 |
| 활성팀 | 팀색 45% 실선 | 팀이름(흰글씨+그림자) |
| 다른팀 | 그 팀색 20% | 작게 |
| 줌 낮음 | 색만 | 라벨 숨김(확대 시) |

- 건물 핀 숨김 (배정은 구역 단위, 핀은 노이즈)
- 진입 시 선택 구 담당 카드에 자동 fit 줌
- 폴리곤 탭 = 활성팀 색칠 (토글). 다른팀 탭 = 활성팀으로 이동 + **"이동됨·실행취소" 토스트**

#### 목록 모드 (구역 + 비공식 + 식당)
```
[sticky 헤더]  ← 고정 (엑셀식)
┌──────────────────────────────┐
│ 🔍 검색      ☑ 미배정만        │
│ ▾ 기흥구·신갈동                │
│ │ ☑ 신갈동4  주택4  25%       │ ← 활성팀 배정(체크+강조)
│ │ □ 신갈동9  주택4  75%       │ ← 미배정
│ │ [팀2] 신갈동11 주택1        │ ← 다른팀(팀이름 pill), 탭→이동
│ ▾ 📁 비공식 카드               │
│ │ □ 비공식A  □ 비공식B        │
│ ▾ 🍴 식당                      │
│ │ □ 중화요리궁                │
└──────────────────────────────┘
```
- 행 = `체크 · 카드명 · 주택N/상가N · 진행률%`
- 다른팀 배정 = 팀이름 pill → 탭하면 활성팀으로 이동 (지도와 동일 규칙)
- 동별 그룹 (기존 step1 재활용), 비공식·식당도 같은 목록 하단 그룹
- page 자연 스크롤 + sticky 헤더 = 엑셀 행고정 동작

#### 모드별 스크롤
| 모드 | 스크롤 | 비공식/식당 |
|---|---|---|
| 목록 | page 자연 스크롤, 헤더 sticky | 같은 목록 하단 그룹 |
| 지도 | 지도 고정창(핀치/드래그) | 없음(목록에서만) + 안내 문구 |

### 3.4 일관성 원칙
- "이 카드 → 어느 팀"을 지도(색+이름)·목록(팀 pill) 동일 표현
- 둘 다 **단일 draft에서 파생**. 지도 전용/목록 전용 state 금지

### 3.5 배정 공유 + draft 관리 (가장 중요)
- **단일 source of truth: `draft.teams[].cardIds`** (+ members, + 비공식/식당 배정). 지도·목록은 순수 파생
- 편집 중: localStorage 자동저장 (Supabase 안 건드림)
- **진입 시: 서버에 이미 shared 배정 있으면 그걸 시작 draft로 로드** (빈 localStorage 우선 X → 다중기기 충돌 완화)
- 오래된 draft 감지: `localStorage draft.updatedAt` vs `event.assignmentSharedAt` 비교 → 서버가 더 최신이면 "다른 곳에서 공유됨, 새로고침?" 경고
- 공유: 일정상세 [배정 공유] → `onAssignCardsToEventParticipantsBulk(eventId, ..., {status:'shared'})` → 공유 후 localStorage draft 정리

---

## 4. 컴포넌트 분해 (재활용 vs 신규)

| 항목 | 처리 |
|---|---|
| team/draft/공유상태 모델 | 그대로 재활용 (백엔드 변경 0) |
| 화면1 팀 짓기 | 신규 (사람 멀티선택→묶기) |
| 화면2 목록 뷰 | 기존 step1 카드피커 재활용 + 비공식/식당 그룹 |
| 화면2 지도 뷰 | 신규 — MapCanvas 폴리곤 색칠/탭 위에 "활성팀→색칠" 연결 |
| sticky 팀바 | 신규 (작음) |
| 구 선택 + 자동줌 | 신규 (작음, MapCanvas fit 활용) |
| 단일 draft reducer | **신규 — 최우선. 지도·목록 공통 진실** |
| 일정상세 진입점 | AdminEventDetailSheet 버튼 (작음) |
| 기존 배정 탭 | **마지막 Phase에서 제거** (안정화까지 병행) |

---

## 5. 빌드 순서 (리스크 감소 시퀀싱)

> **목록 먼저 = 지도가 덜 중요해서가 아니라, 단일 draft reducer 아키텍처를 단순한 UI에서 먼저 못박기 위함.** 그게 단단하면 지도는 순수 파생 뷰로 끼우면 "그냥 동작". 지도는 1급 기능으로 유지.

| Phase | 작업 | 규모 |
|---|---|---|
| 1 | 일정상세 진입점 + 화면1 팀 짓기 + **단일 draft reducer** | 중 (2~3일) |
| 2 | 화면2 목록 뷰 + 비공식/식당 + 서버상태 진입로드 + 공유 | 중 (2일) |
| 3 | 화면2 지도 뷰 (색칠/이름라벨/구선택/자동줌/Undo토스트) | 중 (2~3일) |
| 4 | 기존 배정 탭 제거 + 권한 + QA | 소~중 (1~2일) |

**총 7~10일.** 데이터/백엔드 변경 없음.

---

## 6. 데이터 사용량 영향 (Supabase 최적화 영향 평가)

- **읽기: 새 fetch 없음.** cards/buildings/cardBoundaries/calendarEvents 모두 이미 로드된 slice 재사용
- **쓰기: 배정 공유 1회** → `refetchCalendar = fetchSlices(['calendar']) ≈ 14KB` (기존 최적화된 slice)
- **편집 중(팀짓기/색칠/토글)은 localStorage** → Supabase 안 건드림
- **지도 타일 = 네이버 API** (Supabase egress 무관, 기존 지도 탭과 같은 쿼터)
- **구현 시 철칙: 토글·팀전환·색칠 시 refetch 금지.** 전부 클라이언트 state로만 (draft 단일 reducer가 보장)
- 결론: Supabase 최적화에 **거의 영향 없음.** 별도 배정 탭 제거로 오히려 진입 fetch 감소

---

## 7. 권한
- 보기전용 인도자(`canEditSelectedEvent === false`)는 [팀 구성 & 배정] 비활성, 색칠/이동 불가, 읽기만
- 기존 `canManageAssignment` 체크 재활용

---

## 8. V2로 미루는 것
- 지난 봉사 골격 복사 (팀 수 + 각 팀 구역 복사, 사람 비움)
- 서버사이드 draft (현재 localStorage + 진입 시 서버 shared 로드로 충분)

---

## 9. 코덱스 1차 리뷰 반영 요약

| 코덱스 지적 | 반영 |
|---|---|
| draft 단일 reducer 최우선 | §3.5, §4에 최우선 원칙 명시 |
| 목록뷰 필수 안전장치 | 지도/목록 둘 다, 목록은 정확수정용 |
| localStorage 다중기기 충돌 | §3.5 진입 시 서버 shared 로드 + 오래된 draft 경고 |
| 5~6일 낙관적 | §5 7~10일로 |
| 빌드순서 목록먼저 | §5 채택 (단 이유 재정의) |
| 즉시이동 위험 | §3.3 Undo 토스트 추가 |
| 권한 누락 | §7 추가 |
| 작은 폴리곤 라벨 | §3.3 줌 낮으면 색만 |
| 두 구 걸침 | 구 선택에 "전체" 옵션 |
| 탭 제거는 마지막 | §5 Phase 4 |

**작성자 추가 (코덱스 미지적):**
- 비공식/식당을 화면1→화면2 이동 (화면1 사람 전용 일관성)
- 지도 위상 방어 (보조 아닌 1급, 목록먼저는 리스크감소 목적)

---

**상태**: v2. 코덱스 2차 리뷰 또는 Phase 1 착수 대기.
