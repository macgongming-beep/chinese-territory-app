# 인수인계 — 2026-08-30

> 다음 작업자(코덱스)용. **먼저 `CLAUDE.md` 를 읽고 이걸 읽을 것.**

## 지금 운영 상태 (실측)

```
EMERGENCY_open_*      26개   ← 쓰기가 열려 있다 (긴급 복구 상태)
TEMP_session_gate_*   86개   ← 살아 있지만 위와 OR 로 합쳐져 지금은 무력
anon 이 부를 수 있는 definer 함수   57개
활성 사용자 62명 · 유효 토큰 24명   ← **이 격차가 이번 사고의 핵심이다**
```

`app_users` 만은 긴급 개방에서 **빼 두었다** — 열어 두면 아무나 관리자 계정을 만든다.

## 무슨 일이 있었나 (같은 실수를 반복하지 않기 위해)

1. anon 쓰기 차단을 운영에 넣었다. 검증도 smoke 도 통과했다.
2. **다음 날 봉사 중에 방문 기록·배정이 저장되지 않았다.**
   세션은 30일 뒤 만료되는데 앱은 localStorage 만 보고 로그인 상태로 뒀다.
   그래서 **토큰이 만료된 줄도 모르고 쓰던 사람이 62명 중 38명**이었다.
3. 긴급히 쓰기를 다시 열었다(EMERGENCY_open_*).

**교훈**: 전환 전에 "새 앱을 받았나" 만 봤고 **"토큰이 있나" 를 안 셌다.**
`select count(distinct user_id) from auth_sessions where expires_at > now()` 한 줄이면 보였다.

이미 고친 것: 세션 **슬라이딩 만료**(쓰면 30일 연장) + **토큰 없으면 로그인 화면**.
→ 새 전도인은 로그인하면 토큰이 생기므로 **이 문제는 다시 안 생긴다.**

---

## 해야 할 일 (급한 순)

### 1. 관리자 화면 함수 셋에 관리자 검사 넣고 grant 되돌리기

지금 **회수만 해 둔 상태**라 화면이 오류를 낸다:
`설정 → 데이터 관리` 의 실행 버튼들.

| 함수 | 지금 |
|---|---|
| `delete_old_visit_histories(text)` | 회수됨. **권한 검사 없음** |
| `cleanup_old_data()` | 회수됨. **권한 검사 없음** |
| `manual_reset_met_units()` | 회수됨. **권한 검사 없음** |

**본보기가 있다**: `supabase/migrations/20260830_1600_guard_visitor_change.sql` 의
`session_can_change_visitor()` 와, `20260828_1200_anon_write_lockdown.sql` 의
`public.session_is_admin()`.

⚠ 함수 안에 이렇게 넣는다 (cron 이 `postgres` 로 부르는 것도 있으니 그 길을 막지 말 것):
```sql
if current_user in ('postgres','supabase_admin','service_role') then
  -- cron·서버 경로. 그대로 통과
elsif not (select public.session_is_admin()) then
  raise exception '관리자만 할 수 있습니다';
end if;
```
`cleanup_old_data` 와 `auto_reset_met_units` 는 **cron.job 에 등록돼 있다.** 확인:
`select jobid, schedule, username, command from cron.job;`

넣은 뒤 `grant execute … to anon;` 으로 되돌리고, 밖에서 **거부되는지** 확인한다.
⚠ **쓰기 함수를 진짜로 불러서 확인하지 말 것.** 막혀 있지 않으면 그 함수가 하는 일이
그대로 일어난다 (실제로 그렇게 운영 설정을 바꿔 버린 적이 있다).
`has_function_privilege('anon', oid, 'execute')` 로 보거나, 롤백 트랜잭션 안에서 한다.

### 2. `not in ('admin')` NULL 함정 — **조사는 끝났다. 둘만 고치면 된다**

`v_role not in ('admin','developer')` 은 `v_role` 이 NULL 이면 **참이 아니라 NULL** 이라
`raise` 를 안 탄다. 즉 **없는 토큰이 그대로 통과한다.**

12개를 전부 확인했다:
- ✅ 7개는 null 처리가 있다 (`create_notice_tx` · `import_building_tx` ·
  `merge_duplicate_buildings_tx` · `rename/purge_user_name_references` ·
  `update_calendar_event_tx` · `update_calendar_event_series_tx`)
- ✅ `delete_informal_asset_secure` 는 `verify_session()` 을 먼저 불러 던지므로 안전
- ⚠ **고쳐야 할 둘**: `update_daily_service_settings` ·
  `update_global_push_quiet_settings` — **다행히 지금 anon 에 회수돼 있다.**
  `if v_role is null or v_role not in (...)` 로 고치고 grant 를 되돌린다.
  → 그래야 `알림 설정`의 전역 항목이 다시 동작한다.
- 낮음: `get_daily_service_settings` · `get_global_push_quiet_settings` 는
  읽기 전용이라 급하지 않다 (설정값은 비밀이 아니다).

### 3. `cleanup-chat-images` Edge Function **배포**

코드는 고쳤다(`!expectedKey ||` 로 fail-closed). **아직 배포 안 했다.**
운영에 `CLEANUP_FUNCTION_KEY` 가 설정돼 있는지도 확인할 것. 없으면 그게 더 급하다.

### 4. `get_login_logs` — 남의 접속 기록을 볼 수 있다

본인·developer 판정이 **클라이언트에만** 있다. 서버에서 확인해야 한다.

### 5. `auto_close_stale_sessions` — 무검사 쓰기

모든 사용자가 부르므로 **회수가 아니라 세션 검사**가 맞다.

### 6. 구조 정리 (안전한 작업. 운영 안 건드림)

```
DesktopMap.tsx        2752
MobileMap.tsx         2439
DesktopTerritory.tsx  2361
```
⚠ **떼기 전에 의존성을 센다. 자식이 쓰는 값 10개 이하일 때만.**

---

## ⛔ 운영 DB 는 건드리지 말 것

아래는 사람이 직접, 확인하고 한다:
- `EMERGENCY_open_*` 26개 제거(=다시 잠그기) — `supabase/tools/_RELOCK_긴급개방_해제.sql`
  ⚠ **활성 사용자와 토큰 보유자 수가 가까워진 뒤에.** 그걸 안 보고 들어가서 사고가 났다
- 위 1·2번 SQL 을 운영에 넣는 것

---

## 이 저장소의 규칙 (어기면 조용히 샌다)

- **`security definer` 함수를 만들면 `revoke`.** PostgreSQL 은 만들 때 PUBLIC 에 준다.
  새로 만들 때마다 `supabase/tools/_AUDIT_anon이_부를수있는_definer.sql` 을 돌린다
- **시험을 쓴 뒤 코드를 일부러 망가뜨려 그 시험만 실패하는지 본다.**
  오늘만 헛도는 시험을 여섯 개 잡았다. 통과하는 시험과 무는 시험은 다르다
- **조립을 보는 시험이 없으면 배선이 끊긴 것을 못 잡는다.**
  편집기 단위 시험은 `initial.visitor` 를 직접 넣어 통과했는데, 정작 화면이 그 값을
  안 넘기고 있었다 — 열었다 저장만 해도 남의 기록이 되는 상태였다
- **화면에만 있는 권한은 권한이 아니다.** 표를 직접 쓰는 경로가 있으면 서버에서 막는다
- **SQL Editor 는 `begin/commit` 을 안 지킨다.** 위험한 마이그레이션은
  `npm run apply:lockdown` (psql `--single-transaction`)
- 비밀번호를 명령줄에 쓰지 않는다 (`.env.local` 의 `SUPABASE_DB_URL`)

## 검증

```bash
npm run build && npm run lint && npm test    # 607개
```
